{
  "name": "gatsby-starter-blog-theme",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "develop": "gatsby develop",
    "start": "npm run clean && gatsby develop",
    "build": "npm run clean && gatsby build",
    "clean": "gatsby clean",
    "deploy": "npm run build --max_old_space_size=10240 && surge ./public kuzure.club"
  },
  "license": "0BSD",
  "dependencies": {
    "gatsby": "^3.6.2",
    "gatsby-theme-blog": "^3.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "theme-ui": "0.7.3"
  }
}
